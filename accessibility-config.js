/**
 * Accessibility Plugin - Configuration File
 * Customize the plugin behavior by modifying the values below
 */

var AccessibilityConfig = {
    // Widget position: 'bottom-left' or 'bottom-right'
    position: 'bottom-right',

    // Button text displayed on the toggle button
    buttonText: 'Accessibility',

    // Show reset button in the panel (true/false)
    showReset: true,

    // Default font size percentage (75, 100, 125, 150, 200)
    fontSize: 100,

    // Default contrast mode: 'normal', 'high', or 'dark'
    contrast: 'normal',

    // Default line height: 'normal' or 'large'
    lineHeight: 'normal',

    // Default letter spacing: 'normal' or 'wide'
    letterSpacing: 'normal',

    // Default font family: 'default', 'sans-serif', 'serif', or 'monospace'
    fontFamily: 'default',

    // Color blindness filter: 'none', 'protanopia', 'deuteranopia', 'tritanopia', 'achromatopsia'
    colorBlindness: 'none',

    // Enable focus indicator (true/false)
    focusIndicator: false,

    // Enable reading guide (true/false)
    readingGuide: false,

    // Enable reading mask - hides text below reading position (true/false)
    readingMask: false,

    // Enable text highlight - highlights text as you read (true/false)
    textHighlight: false,

    // Stop animations (true/false)
    stopAnimations: false,

    // Underline all links (true/false)
    underlineLinks: false,

    // Show image alt text (true/false)
    showImageAlt: false,

    // Enable text-to-speech (true/false)
    ttsEnabled: false,

    // TTS settings (0.5 to 2.0)
    ttsRate: 1.0,
    ttsPitch: 1.0,
    ttsVolume: 1.0,

    // TTS voice (empty string for default, or specific voice name)
    ttsVoice: 'default',
    
    // TTS language: 'en' (English), 'es' (Español), 'de' (Deutsch), 'fr' (Français), 'zh' (中文/Mandarin), 'ko' (한국어), 'no' (Norsk)
    ttsLanguage: 'en'
};

